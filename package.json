{
	"name": "hm-scroll-synced-zoom-block",
	"version": "1.0.0",
	"description": "A block that scales its contents on scroll, optionally pinning into a modal-style spotlight.",
	"author": "Human Made Limited",
	"license": "GPL-2.0-or-later",
	"main": "build/index.js",
	"scripts": {
		"build": "wp-scripts build",
		"start": "wp-scripts start",
		"format": "wp-scripts format",
		"lint:css": "wp-scripts lint-style",
		"lint:js": "wp-scripts lint-js",
		"plugin-zip": "wp-scripts plugin-zip",
		"release": "git fetch origin && git checkout release && git reset origin/main --hard && npm run build && git add -f build && git commit -m 'Release build' --no-verify && git push origin release --force && git checkout -"
	},
	"devDependencies": {
		"@wordpress/scripts": "^30.10.0"
	}
}
